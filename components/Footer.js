import styled from "styled-components";

const Footer = () => {
    return <FooterContainer>푸토</FooterContainer>;
};

const FooterContainer = styled.div`
    height: 2500px;
`;

export default Footer;
