import ModalLayer from "@/components/ModalLayer";

const ProjectDetailModal = () => {
  return (
    <ModalLayer>
      <dialog open id="dialog-1" className="modal-bg-box left-[50%] translate-x-[-50%]">
        <h1>TEST!!!!</h1>
      </dialog>
    </ModalLayer>
  );
};

export default ProjectDetailModal;
